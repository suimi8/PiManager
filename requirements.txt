# 运行时依赖（进入 PyInstaller 产物），用 == 精确固定以保证可复现构建。
# For production deployments, consider using --require-hashes with a
# requirements.txt.sha256 file for supply-chain integrity verification.
#
# 构建工具（PyInstaller）与开发/CI 工具见 requirements-dev.txt：PyInstaller 不是
# 运行时依赖，放在这里曾让 README/BUILD.md 写出 `-r requirements.txt pyinstaller`
# 这种「已 pin 又追加无约束版本」的冗余命令。
PySide6==6.11.1
PyYAML==6.0.3
certifi==2026.7.22
keyring==25.7.0
cryptography==50.0.0
