# Dependencies are pinned with == for reproducible builds.
# For production deployments, consider using --require-hashes with a
# requirements.txt.sha256 file for supply-chain integrity verification.
PySide6==6.11.1
PyYAML==6.0.2
certifi==2026.7.22
keyring==25.7.0
cryptography==50.0.0
PyInstaller==6.19.0
